#version 120

uniform sampler2D source;
uniform float clampValue;

varying vec2 pos;

void main()
{
	vec4 color  = texture2D(source, pos);
	if(dot(color,color) < 4*clampValue) discard;
	gl_FragColor = vec4(1.0);
}
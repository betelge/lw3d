#version 120

uniform sampler2D texture0;
uniform vec3 lightPos;

varying vec3 pos;
varying vec2 tc;
varying vec3 N;

void main()
{
	gl_FragColor = texture2D(texture0, tc) * clamp(dot(normalize(N), normalize(lightPos-pos)), 0.0, 1.0 );
}
